import { TypeChangeInputDirective } from './type-change-input.directive';

describe('TypeChangeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeChangeInputDirective();
    expect(directive).toBeTruthy();
  });
});
